/**
 * AgentResume Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { U32, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface AgentResumeData {
	agentData: {
		agentId?: string
		sessionId?: string
		serialNum: number
	}
}

export const agentResumeMetadata = {
	id: 79,
	name: "AgentResume",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
				["serialNum", U32],
			],
		},
	],
} satisfies PacketMetadata

export const agentResume =
	createPacketSender<AgentResumeData>(agentResumeMetadata)

export const createAgentResumeDelegate =
	createPacketDelegate<AgentResumeData>(agentResumeMetadata)
