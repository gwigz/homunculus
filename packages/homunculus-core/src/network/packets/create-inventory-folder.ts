/**
 * CreateInventoryFolder Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S8, UUID, Variable1 } from "../types"

export interface CreateInventoryFolderData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	folderData?: {
		folderId: string
		parentId: string
		type: number
		name: string | Buffer
	}
}

export const createInventoryFolderMetadata = {
	id: 273,
	name: "CreateInventoryFolder",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "folderData",
			parameters: [
				["folderId", UUID],
				["parentId", UUID],
				["type", S8],
				["name", Variable1],
			],
		},
	],
} satisfies PacketMetadata

export const createInventoryFolder =
	createPacketSender<CreateInventoryFolderData>(createInventoryFolderMetadata)

export const createCreateInventoryFolderDelegate =
	createPacketDelegate<CreateInventoryFolderData>(createInventoryFolderMetadata)
