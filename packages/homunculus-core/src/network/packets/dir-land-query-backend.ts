/**
 * DirLandQueryBackend Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { Bool, S32, U32, UUID } from "../types"

export interface DirLandQueryBackendData {
	agentData?: {
		agentId?: string
	}
	queryData?: {
		queryId: string
		queryFlags: number
		searchType: number
		price: number
		area: number
		queryStart: number
		estateId: number
		godlike: boolean
	}
}

export const dirLandQueryBackendMetadata = {
	id: 49,
	name: "DirLandQueryBackend",
	frequency: 2,
	trusted: true,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [["agentId", UUID]],
		},
		{
			name: "queryData",
			parameters: [
				["queryId", UUID],
				["queryFlags", U32],
				["searchType", U32],
				["price", S32],
				["area", S32],
				["queryStart", S32],
				["estateId", U32],
				["godlike", Bool],
			],
		},
	],
} satisfies PacketMetadata

export const dirLandQueryBackend = createPacketSender<DirLandQueryBackendData>(
	dirLandQueryBackendMetadata,
)

export const createDirLandQueryBackendDelegate =
	createPacketDelegate<DirLandQueryBackendData>(dirLandQueryBackendMetadata)
