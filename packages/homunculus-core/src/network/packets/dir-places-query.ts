/**
 * DirPlacesQuery Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S8, S32, U32, UUID, Variable1 } from "../types"

export interface DirPlacesQueryData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	queryData?: {
		queryId: string
		queryText: string | Buffer
		queryFlags: number
		category: number
		simName: string | Buffer
		queryStart: number
	}
}

export const dirPlacesQueryMetadata = {
	id: 33,
	name: "DirPlacesQuery",
	frequency: 2,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "queryData",
			parameters: [
				["queryId", UUID],
				["queryText", Variable1],
				["queryFlags", U32],
				["category", S8],
				["simName", Variable1],
				["queryStart", S32],
			],
		},
	],
} satisfies PacketMetadata

export const dirPlacesQuery = createPacketSender<DirPlacesQueryData>(
	dirPlacesQueryMetadata,
)

export const createDirPlacesQueryDelegate =
	createPacketDelegate<DirPlacesQueryData>(dirPlacesQueryMetadata)
