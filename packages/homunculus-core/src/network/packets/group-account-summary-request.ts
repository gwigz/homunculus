/**
 * GroupAccountSummaryRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S32, UUID } from "../types"

export interface GroupAccountSummaryRequestData {
	agentData: {
		agentId?: string
		sessionId?: string
		groupId: string
	}
	moneyData?: {
		requestId: string
		intervalDays: number
		currentInterval: number
	}
}

export const groupAccountSummaryRequestMetadata = {
	id: 353,
	name: "GroupAccountSummaryRequest",
	frequency: 2,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
				["groupId", UUID],
			],
		},
		{
			name: "moneyData",
			parameters: [
				["requestId", UUID],
				["intervalDays", S32],
				["currentInterval", S32],
			],
		},
	],
} satisfies PacketMetadata

export const groupAccountSummaryRequest =
	createPacketSender<GroupAccountSummaryRequestData>(
		groupAccountSummaryRequestMetadata,
	)

export const createGroupAccountSummaryRequestDelegate =
	createPacketDelegate<GroupAccountSummaryRequestData>(
		groupAccountSummaryRequestMetadata,
	)
