/**
 * GroupRoleChanges Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { U32, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface GroupRoleChangesData {
	agentData: {
		agentId?: string
		sessionId?: string
		groupId: string
	}
	roleChange?: {
		roleId: string
		memberId: string
		change: number
	}[]
}

export const groupRoleChangesMetadata = {
	id: 342,
	name: "GroupRoleChanges",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
				["groupId", UUID],
			],
		},
		{
			name: "roleChange",
			parameters: [
				["roleId", UUID],
				["memberId", UUID],
				["change", U32],
			],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const groupRoleChanges = createPacketSender<GroupRoleChangesData>(
	groupRoleChangesMetadata,
)

export const createGroupRoleChangesDelegate =
	createPacketDelegate<GroupRoleChangesData>(groupRoleChangesMetadata)
