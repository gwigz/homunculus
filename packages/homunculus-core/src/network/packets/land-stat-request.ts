/**
 * LandStatRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S32, U32, UUID, Variable1 } from "../types"

export interface LandStatRequestData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	requestData?: {
		reportType: number
		requestFlags: number
		filter: string | Buffer
		parcelLocalId: number
	}
}

export const landStatRequestMetadata = {
	id: 421,
	name: "LandStatRequest",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "requestData",
			parameters: [
				["reportType", U32],
				["requestFlags", U32],
				["filter", Variable1],
				["parcelLocalId", S32],
			],
		},
	],
} satisfies PacketMetadata

export const landStatRequest = createPacketSender<LandStatRequestData>(
	landStatRequestMetadata,
)

export const createLandStatRequestDelegate =
	createPacketDelegate<LandStatRequestData>(landStatRequestMetadata)
