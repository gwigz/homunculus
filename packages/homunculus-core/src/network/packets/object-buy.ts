/**
 * ObjectBuy Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { S32, U8, U32, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface ObjectBuyData {
	agentData: {
		agentId?: string
		sessionId?: string
		groupId: string
		categoryId: string
	}
	objectData?: {
		objectLocalId: number
		saleType: number
		salePrice: number
	}[]
}

export const objectBuyMetadata = {
	id: 102,
	name: "ObjectBuy",
	frequency: 2,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
				["groupId", UUID],
				["categoryId", UUID],
			],
		},
		{
			name: "objectData",
			parameters: [
				["objectLocalId", U32],
				["saleType", U8],
				["salePrice", S32],
			],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const objectBuy = createPacketSender<ObjectBuyData>(objectBuyMetadata)

export const createObjectBuyDelegate =
	createPacketDelegate<ObjectBuyData>(objectBuyMetadata)
