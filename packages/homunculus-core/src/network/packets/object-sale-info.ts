/**
 * ObjectSaleInfo Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S32, U8, U32, UUID } from "../types"

export interface ObjectSaleInfoData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	objectData?: {
		localId: number
		saleType: number
		salePrice: number
	}[]
}

export const objectSaleInfoMetadata = {
	id: 106,
	name: "ObjectSaleInfo",
	frequency: 2,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "objectData",
			parameters: [
				["localId", U32],
				["saleType", U8],
				["salePrice", S32],
			],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const objectSaleInfo = createPacketSender<ObjectSaleInfoData>(
	objectSaleInfoMetadata,
)

export const createObjectSaleInfoDelegate =
	createPacketDelegate<ObjectSaleInfoData>(objectSaleInfoMetadata)
