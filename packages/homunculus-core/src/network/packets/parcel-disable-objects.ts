/**
 * ParcelDisableObjects Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { S32, U32, UUID } from "../types"

export interface ParcelDisableObjectsData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	parcelData?: {
		localId: number
		returnType: number
	}
	taskIDs?: {
		taskId: string
	}[]
	ownerIDs?: {
		ownerId: string
	}[]
}

export const parcelDisableObjectsMetadata = {
	id: 201,
	name: "ParcelDisableObjects",
	frequency: 2,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "parcelData",
			parameters: [
				["localId", S32],
				["returnType", U32],
			],
		},
		{
			name: "taskIDs",
			parameters: [["taskId", UUID]],
			multiple: true,
		},
		{
			name: "ownerIDs",
			parameters: [["ownerId", UUID]],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const parcelDisableObjects =
	createPacketSender<ParcelDisableObjectsData>(parcelDisableObjectsMetadata)

export const createParcelDisableObjectsDelegate =
	createPacketDelegate<ParcelDisableObjectsData>(parcelDisableObjectsMetadata)
