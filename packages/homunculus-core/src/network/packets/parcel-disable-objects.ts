/**
 * ParcelDisableObjects Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import * as Types from "~/network/types"
import { Packet } from "./packet"

export interface ParcelDisableObjectsData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	parcelData?: {
		localId: number
		returnType: number
	}
	taskIDs?: {
		taskId: string
	}[]
	ownerIDs?: {
		ownerId: string
	}[]
}

export class ParcelDisableObjects extends Packet<ParcelDisableObjectsData> {
	public static override id = 201
	public static override frequency = 0
	public static override trusted = false
	public static override compression = true

	public static override format = new Map([
		[
			"agentData",
			{
				quantity: 1,
				parameters: new Map<string, Types.Type>([
					["agentId", Types.UUID],
					["sessionId", Types.UUID],
				]),
			},
		],
		[
			"parcelData",
			{
				quantity: 1,
				parameters: new Map<string, Types.Type>([
					["localId", Types.S32],
					["returnType", Types.U32],
				]),
			},
		],
		[
			"taskIDs",
			{
				parameters: new Map<string, Types.Type>([["taskId", Types.UUID]]),
			},
		],
		[
			"ownerIDs",
			{
				parameters: new Map<string, Types.Type>([["ownerId", Types.UUID]]),
			},
		],
	])
}
