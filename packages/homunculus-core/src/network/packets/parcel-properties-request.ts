/**
 * ParcelPropertiesRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { Bool, F32, S32, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface ParcelPropertiesRequestData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	parcelData?: {
		sequenceId: number
		west: number
		south: number
		east: number
		north: number
		snapSelection: boolean
	}
}

export const parcelPropertiesRequestMetadata = {
	id: 11,
	name: "ParcelPropertiesRequest",
	frequency: 1,
	compression: true,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "parcelData",
			parameters: [
				["sequenceId", S32],
				["west", F32],
				["south", F32],
				["east", F32],
				["north", F32],
				["snapSelection", Bool],
			],
		},
	],
} satisfies PacketMetadata

export const parcelPropertiesRequest =
	createPacketSender<ParcelPropertiesRequestData>(
		parcelPropertiesRequestMetadata,
	)

export const createParcelPropertiesRequestDelegate =
	createPacketDelegate<ParcelPropertiesRequestData>(
		parcelPropertiesRequestMetadata,
	)
