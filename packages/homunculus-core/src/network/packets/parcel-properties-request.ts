/**
 * ParcelPropertiesRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import * as Types from "../types"
import { Packet } from "./packet"

export interface ParcelPropertiesRequestData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	parcelData?: {
		sequenceId: number
		west: number
		south: number
		east: number
		north: number
		snapSelection: boolean
	}
}

export class ParcelPropertiesRequest extends Packet<ParcelPropertiesRequestData> {
	public static override id = 11
	public static override frequency = 1
	public static override trusted = false
	public static override compression = true

	public static override format = new Map([
		[
			"agentData",
			{
				quantity: 1,
				parameters: new Map<string, Types.Type>([
					["agentId", Types.UUID],
					["sessionId", Types.UUID],
				]),
			},
		],
		[
			"parcelData",
			{
				quantity: 1,
				parameters: new Map<string, Types.Type>([
					["sequenceId", Types.S32],
					["west", Types.F32],
					["south", Types.F32],
					["east", Types.F32],
					["north", Types.F32],
					["snapSelection", Types.Bool],
				]),
			},
		],
	])
}
