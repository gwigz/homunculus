/**
 * RemoveInventoryObjects Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface RemoveInventoryObjectsData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	folderData?: {
		folderId: string
	}[]
	itemData?: {
		itemId: string
	}[]
}

export const removeInventoryObjectsMetadata = {
	id: 284,
	name: "RemoveInventoryObjects",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "folderData",
			parameters: [["folderId", UUID]],
			multiple: true,
		},
		{
			name: "itemData",
			parameters: [["itemId", UUID]],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const removeInventoryObjects =
	createPacketSender<RemoveInventoryObjectsData>(removeInventoryObjectsMetadata)

export const createRemoveInventoryObjectsDelegate =
	createPacketDelegate<RemoveInventoryObjectsData>(
		removeInventoryObjectsMetadata,
	)
