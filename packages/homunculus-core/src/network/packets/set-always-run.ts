/**
 * SetAlwaysRun Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "../packet"
import { Bool, UUID } from "../types"

export interface SetAlwaysRunData {
	agentData: {
		agentId?: string
		sessionId?: string
		alwaysRun: boolean
	}
}

export const setAlwaysRunMetadata = {
	id: 88,
	name: "SetAlwaysRun",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
				["alwaysRun", Bool],
			],
		},
	],
} satisfies PacketMetadata

export const setAlwaysRun =
	createPacketSender<SetAlwaysRunData>(setAlwaysRunMetadata)

export const createSetAlwaysRunDelegate =
	createPacketDelegate<SetAlwaysRunData>(setAlwaysRunMetadata)
