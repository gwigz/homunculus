/**
 * SetScriptRunning Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { Bool, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface SetScriptRunningData {
	agentData?: {
		agentId?: string
		sessionId?: string
	}
	script?: {
		objectId: string
		itemId: string
		running: boolean
	}
}

export const setScriptRunningMetadata = {
	id: 245,
	name: "SetScriptRunning",
	frequency: 2,
	blocks: [
		{
			name: "agentData",
			parameters: [
				["agentId", UUID],
				["sessionId", UUID],
			],
		},
		{
			name: "script",
			parameters: [
				["objectId", UUID],
				["itemId", UUID],
				["running", Bool],
			],
		},
	],
} satisfies PacketMetadata

export const setScriptRunning = createPacketSender<SetScriptRunningData>(
	setScriptRunningMetadata,
)

export const createSetScriptRunningDelegate =
	createPacketDelegate<SetScriptRunningData>(setScriptRunningMetadata)
