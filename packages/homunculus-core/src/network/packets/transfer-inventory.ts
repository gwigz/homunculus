/**
 * TransferInventory Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import * as Types from "../types"
import { Packet } from "./packet"

export interface TransferInventoryData {
	infoBlock?: {
		sourceId: string | Buffer
		destId: string | Buffer
		transactionId: string | Buffer
	}
	inventoryBlock?: {
		inventoryId: string | Buffer
		type: number
	}[]
}

export class TransferInventory extends Packet<TransferInventoryData> {
	public static override id = 295
	public static override frequency = 0
	public static override trusted = true
	public static override compression = true

	public static override format = new Map([
		[
			"infoBlock",
			{
				quantity: 1,
				parameters: new Map<string, Types.Type>([
					["sourceId", Types.UUID],
					["destId", Types.UUID],
					["transactionId", Types.UUID],
				]),
			},
		],
		[
			"inventoryBlock",
			{
				parameters: new Map<string, Types.Type>([
					["inventoryId", Types.UUID],
					["type", Types.S8],
				]),
			},
		],
	])
}
