/**
 * TransferInventory Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import { S8, UUID } from "../types"
import {
	createPacketDelegate,
	createPacketSender,
	type PacketMetadata,
} from "./packet"

export interface TransferInventoryData {
	infoBlock?: {
		sourceId: string
		destId: string
		transactionId: string
	}
	inventoryBlock?: {
		inventoryId: string
		type: number
	}[]
}

export const transferInventoryMetadata = {
	id: 295,
	name: "TransferInventory",
	frequency: 2,
	trusted: true,
	compression: true,
	blocks: [
		{
			name: "infoBlock",
			parameters: [
				["sourceId", UUID],
				["destId", UUID],
				["transactionId", UUID],
			],
		},
		{
			name: "inventoryBlock",
			parameters: [
				["inventoryId", UUID],
				["type", S8],
			],
			multiple: true,
		},
	],
} satisfies PacketMetadata

export const transferInventory = createPacketSender<TransferInventoryData>(
	transferInventoryMetadata,
)

export const createTransferInventoryDelegate =
	createPacketDelegate<TransferInventoryData>(transferInventoryMetadata)
