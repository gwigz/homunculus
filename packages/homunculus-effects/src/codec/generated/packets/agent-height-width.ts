/**
 * AgentHeightWidth Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface AgentHeightWidthData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
		circuitCode?: number
	}
	heightWidthBlock: {
		genCounter: number
		height: number
		width: number
	}
}

export const id = 83
export const name = "AgentHeightWidth"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() + // sessionId
	Primitives.U32.size() // circuitCode

// base byte size for one "heightWidthBlock" entry (fixed-length parameters only)
const HEIGHT_WIDTH_BLOCK_BASE_SIZE =
	Primitives.U32.size() + // genCounter
	Primitives.U16.size() + // height
	Primitives.U16.size() // width

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + HEIGHT_WIDTH_BLOCK_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<AgentHeightWidthData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)
	offset = Primitives.U32.encode(data.agentData.circuitCode, buffer, offset)

	// HeightWidthBlock
	offset = Primitives.U32.encode(data.heightWidthBlock.genCounter, buffer, offset)
	offset = Primitives.U16.encode(data.heightWidthBlock.height, buffer, offset)
	offset = Primitives.U16.encode(data.heightWidthBlock.width, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): AgentHeightWidthData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
			circuitCode: Primitives.U32.decode(buffer, state),
		},
		heightWidthBlock: {
			genCounter: Primitives.U32.decode(buffer, state),
			height: Primitives.U16.decode(buffer, state),
			width: Primitives.U16.decode(buffer, state),
		},
	}
}
