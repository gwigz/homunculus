/**
 * AgentMovementComplete Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface AgentMovementCompleteData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	data: {
		position: Types.Vector3
		lookAt: Types.Vector3
		regionHandle: number | bigint
		timestamp: number
	}
	simData: {
		channelVersion: Buffer
	}
}

export const id = 250
export const name = "AgentMovementComplete"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "data" entry (fixed-length parameters only)
const DATA_BASE_SIZE =
	Primitives.Vector3.size() + // position
	Primitives.Vector3.size() + // lookAt
	Primitives.U64.size() + // regionHandle
	Primitives.U32.size() // timestamp

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + DATA_BASE_SIZE + SIM_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<AgentMovementCompleteData>) {
	let size = BASE_SIZE

	size += Primitives.Variable2.size(data.simData.channelVersion)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// Data
	offset = Primitives.Vector3.encode(data.data.position, buffer, offset)
	offset = Primitives.Vector3.encode(data.data.lookAt, buffer, offset)
	offset = Primitives.U64.encode(data.data.regionHandle, buffer, offset)
	offset = Primitives.U32.encode(data.data.timestamp, buffer, offset)

	// SimData
	offset = Primitives.Variable2.encode(data.simData.channelVersion, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): AgentMovementCompleteData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		data: {
			position: Primitives.Vector3.decode(buffer, state),
			lookAt: Primitives.Vector3.decode(buffer, state),
			regionHandle: Primitives.U64.decode(buffer, state),
			timestamp: Primitives.U32.decode(buffer, state),
		},
		simData: {
			channelVersion: Primitives.Variable2.decode(buffer, state),
		},
	}
}
