/**
 * AvatarInterestsUpdate Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface AvatarInterestsUpdateData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	propertiesData: {
		wantToMask: number
		wantToText: Buffer
		skillsMask: number
		skillsText: Buffer
		languagesText: Buffer
	}
}

export const id = 175
export const name = "AvatarInterestsUpdate"
export const frequency = 2
export const compression = true

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "propertiesData" entry (fixed-length parameters only)
const PROPERTIES_DATA_BASE_SIZE =
	Primitives.U32.size() + // wantToMask
	Primitives.U32.size() // skillsMask

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + PROPERTIES_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<AvatarInterestsUpdateData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.propertiesData.wantToText)
	size += Primitives.Variable1.size(data.propertiesData.skillsText)
	size += Primitives.Variable1.size(data.propertiesData.languagesText)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// PropertiesData
	offset = Primitives.U32.encode(data.propertiesData.wantToMask, buffer, offset)
	offset = Primitives.Variable1.encode(data.propertiesData.wantToText, buffer, offset)
	offset = Primitives.U32.encode(data.propertiesData.skillsMask, buffer, offset)
	offset = Primitives.Variable1.encode(data.propertiesData.skillsText, buffer, offset)
	offset = Primitives.Variable1.encode(data.propertiesData.languagesText, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): AvatarInterestsUpdateData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		propertiesData: {
			wantToMask: Primitives.U32.decode(buffer, state),
			wantToText: Primitives.Variable1.decode(buffer, state),
			skillsMask: Primitives.U32.decode(buffer, state),
			skillsText: Primitives.Variable1.decode(buffer, state),
			languagesText: Primitives.Variable1.decode(buffer, state),
		},
	}
}
