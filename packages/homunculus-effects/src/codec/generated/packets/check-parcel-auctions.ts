/**
 * CheckParcelAuctions Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"

export interface CheckParcelAuctionsData {
	regionData: {
		regionHandle: number | bigint
	}[]
}

export const id = 233
export const name = "CheckParcelAuctions"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "regionData" entry (fixed-length parameters only)
const REGION_DATA_BASE_SIZE = Primitives.U64.size() // regionHandle

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<CheckParcelAuctionsData>) {
	let size = BASE_SIZE

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += REGION_DATA_BASE_SIZE * (data.regionData?.length ?? 0)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// RegionData
	offset = Primitives.U8.encode(data.regionData?.length ?? 0, buffer, offset)

	for (const item of data.regionData ?? []) {
		offset = Primitives.U64.encode(item.regionHandle, buffer, offset)
	}

	return buffer
}

export function decode(buffer: Buffer): CheckParcelAuctionsData {
	const state = { offset: HEADER_SIZE }

	return {
		regionData: (() => {
			const items: CheckParcelAuctionsData["regionData"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					regionHandle: Primitives.U64.decode(buffer, state),
				})
			}

			return items
		})(),
	}
}
