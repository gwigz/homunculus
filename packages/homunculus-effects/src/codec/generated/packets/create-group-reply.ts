/**
 * CreateGroupReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface CreateGroupReplyData {
	agentData?: {
		agentId?: Types.UUID
	}
	replyData: {
		groupId: Types.UUID
		success: boolean
		message: Buffer
	}
}

export const id = 340
export const name = "CreateGroupReply"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE = Primitives.UUID.size() // agentId

// base byte size for one "replyData" entry (fixed-length parameters only)
const REPLY_DATA_BASE_SIZE =
	Primitives.UUID.size() + // groupId
	Primitives.Boolean.size() // success

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + REPLY_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<CreateGroupReplyData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.replyData.message)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)

	// ReplyData
	offset = Primitives.UUID.encode(data.replyData.groupId, buffer, offset)
	offset = Primitives.Boolean.encode(data.replyData.success, buffer, offset)
	offset = Primitives.Variable1.encode(data.replyData.message, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): CreateGroupReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
		},
		replyData: {
			groupId: Primitives.UUID.decode(buffer, state),
			success: Primitives.Boolean.decode(buffer, state),
			message: Primitives.Variable1.decode(buffer, state),
		},
	}
}
