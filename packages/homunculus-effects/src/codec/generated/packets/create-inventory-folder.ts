/**
 * CreateInventoryFolder Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface CreateInventoryFolderData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	folderData: {
		folderId: Types.UUID
		parentId: Types.UUID
		type: number
		name: Buffer
	}
}

export const id = 273
export const name = "CreateInventoryFolder"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "folderData" entry (fixed-length parameters only)
const FOLDER_DATA_BASE_SIZE =
	Primitives.UUID.size() + // folderId
	Primitives.UUID.size() + // parentId
	Primitives.S8.size() // type

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + FOLDER_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<CreateInventoryFolderData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.folderData.name)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// FolderData
	offset = Primitives.UUID.encode(data.folderData.folderId, buffer, offset)
	offset = Primitives.UUID.encode(data.folderData.parentId, buffer, offset)
	offset = Primitives.S8.encode(data.folderData.type, buffer, offset)
	offset = Primitives.Variable1.encode(data.folderData.name, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): CreateInventoryFolderData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		folderData: {
			folderId: Primitives.UUID.decode(buffer, state),
			parentId: Primitives.UUID.decode(buffer, state),
			type: Primitives.S8.decode(buffer, state),
			name: Primitives.Variable1.decode(buffer, state),
		},
	}
}
