/**
 * DataServerLogout Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface DataServerLogoutData {
	userData: {
		agentId: Types.UUID
		viewerIp: Types.IP
		disconnect: boolean
		sessionId: Types.UUID
	}
}

export const id = 251
export const name = "DataServerLogout"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "userData" entry (fixed-length parameters only)
const USER_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.IP.size() + // viewerIp
	Primitives.Boolean.size() + // disconnect
	Primitives.UUID.size() // sessionId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + USER_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<DataServerLogoutData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// UserData
	offset = Primitives.UUID.encode(data.userData.agentId, buffer, offset)
	offset = Primitives.IP.encode(data.userData.viewerIp, buffer, offset)
	offset = Primitives.Boolean.encode(data.userData.disconnect, buffer, offset)
	offset = Primitives.UUID.encode(data.userData.sessionId, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): DataServerLogoutData {
	const state = { offset: HEADER_SIZE }

	return {
		userData: {
			agentId: Primitives.UUID.decode(buffer, state),
			viewerIp: Primitives.IP.decode(buffer, state),
			disconnect: Primitives.Boolean.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
	}
}
