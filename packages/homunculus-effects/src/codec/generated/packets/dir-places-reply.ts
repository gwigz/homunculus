/**
 * DirPlacesReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface DirPlacesReplyData {
	agentData?: {
		agentId?: Types.UUID
	}
	queryData: {
		queryId: Types.UUID
	}[]
	queryReplies: {
		parcelId: Types.UUID
		name: Buffer
		forSale: boolean
		auction: boolean
		dwell: number
	}[]
	statusData: {
		status: number
	}[]
}

export const id = 35
export const name = "DirPlacesReply"
export const frequency = 2
export const compression = true

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE = Primitives.UUID.size() // agentId

// base byte size for one "queryData" entry (fixed-length parameters only)
const QUERY_DATA_BASE_SIZE = Primitives.UUID.size() // queryId

// base byte size for one "queryReplies" entry (fixed-length parameters only)
const QUERY_REPLIES_BASE_SIZE =
	Primitives.UUID.size() + // parcelId
	Primitives.Boolean.size() + // forSale
	Primitives.Boolean.size() + // auction
	Primitives.F32.size() // dwell

// base byte size for one "statusData" entry (fixed-length parameters only)
const STATUS_DATA_BASE_SIZE = Primitives.U32.size() // status

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<DirPlacesReplyData>) {
	let size = BASE_SIZE

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += QUERY_DATA_BASE_SIZE * (data.queryData?.length ?? 0)

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += QUERY_REPLIES_BASE_SIZE * (data.queryReplies?.length ?? 0)

	for (const item of data.queryReplies ?? []) {
		size += Primitives.Variable1.size(item.name)
	}

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += STATUS_DATA_BASE_SIZE * (data.statusData?.length ?? 0)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)

	// QueryData
	offset = Primitives.U8.encode(data.queryData?.length ?? 0, buffer, offset)

	for (const item of data.queryData ?? []) {
		offset = Primitives.UUID.encode(item.queryId, buffer, offset)
	}

	// QueryReplies
	offset = Primitives.U8.encode(data.queryReplies?.length ?? 0, buffer, offset)

	for (const item of data.queryReplies ?? []) {
		offset = Primitives.UUID.encode(item.parcelId, buffer, offset)
		offset = Primitives.Variable1.encode(item.name, buffer, offset)
		offset = Primitives.Boolean.encode(item.forSale, buffer, offset)
		offset = Primitives.Boolean.encode(item.auction, buffer, offset)
		offset = Primitives.F32.encode(item.dwell, buffer, offset)
	}

	// StatusData
	offset = Primitives.U8.encode(data.statusData?.length ?? 0, buffer, offset)

	for (const item of data.statusData ?? []) {
		offset = Primitives.U32.encode(item.status, buffer, offset)
	}

	return buffer
}

export function decode(buffer: Buffer): DirPlacesReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
		},
		queryData: (() => {
			const items: DirPlacesReplyData["queryData"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					queryId: Primitives.UUID.decode(buffer, state),
				})
			}

			return items
		})(),
		queryReplies: (() => {
			const items: DirPlacesReplyData["queryReplies"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					parcelId: Primitives.UUID.decode(buffer, state),
					name: Primitives.Variable1.decode(buffer, state),
					forSale: Primitives.Boolean.decode(buffer, state),
					auction: Primitives.Boolean.decode(buffer, state),
					dwell: Primitives.F32.decode(buffer, state),
				})
			}

			return items
		})(),
		statusData: (() => {
			const items: DirPlacesReplyData["statusData"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					status: Primitives.U32.decode(buffer, state),
				})
			}

			return items
		})(),
	}
}
