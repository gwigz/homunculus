/**
 * EmailMessageRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface EmailMessageRequestData {
	dataBlock: {
		objectId: Types.UUID
		fromAddress: Buffer
		subject: Buffer
	}
}

export const id = 335
export const name = "EmailMessageRequest"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "dataBlock" entry (fixed-length parameters only)
const DATA_BLOCK_BASE_SIZE = Primitives.UUID.size() // objectId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + DATA_BLOCK_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<EmailMessageRequestData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.dataBlock.fromAddress)
	size += Primitives.Variable1.size(data.dataBlock.subject)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// DataBlock
	offset = Primitives.UUID.encode(data.dataBlock.objectId, buffer, offset)
	offset = Primitives.Variable1.encode(data.dataBlock.fromAddress, buffer, offset)
	offset = Primitives.Variable1.encode(data.dataBlock.subject, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): EmailMessageRequestData {
	const state = { offset: HEADER_SIZE }

	return {
		dataBlock: {
			objectId: Primitives.UUID.decode(buffer, state),
			fromAddress: Primitives.Variable1.decode(buffer, state),
			subject: Primitives.Variable1.decode(buffer, state),
		},
	}
}
