/**
 * EstateCovenantReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface EstateCovenantReplyData {
	data: {
		covenantId: Types.UUID
		covenantTimestamp: number
		estateName: Buffer
		estateOwnerId: Types.UUID
	}
}

export const id = 204
export const name = "EstateCovenantReply"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "data" entry (fixed-length parameters only)
const DATA_BASE_SIZE =
	Primitives.UUID.size() + // covenantId
	Primitives.U32.size() + // covenantTimestamp
	Primitives.UUID.size() // estateOwnerId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<EstateCovenantReplyData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.data.estateName)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// Data
	offset = Primitives.UUID.encode(data.data.covenantId, buffer, offset)
	offset = Primitives.U32.encode(data.data.covenantTimestamp, buffer, offset)
	offset = Primitives.Variable1.encode(data.data.estateName, buffer, offset)
	offset = Primitives.UUID.encode(data.data.estateOwnerId, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): EstateCovenantReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		data: {
			covenantId: Primitives.UUID.decode(buffer, state),
			covenantTimestamp: Primitives.U32.decode(buffer, state),
			estateName: Primitives.Variable1.decode(buffer, state),
			estateOwnerId: Primitives.UUID.decode(buffer, state),
		},
	}
}
