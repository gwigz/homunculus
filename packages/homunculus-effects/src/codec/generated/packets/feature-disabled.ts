/**
 * FeatureDisabled Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface FeatureDisabledData {
	failureInfo: {
		errorMessage: Buffer
		agentId: Types.UUID
		transactionId: Types.UUID
	}
}

export const id = 19
export const name = "FeatureDisabled"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "failureInfo" entry (fixed-length parameters only)
const FAILURE_INFO_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // transactionId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + FAILURE_INFO_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<FeatureDisabledData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.failureInfo.errorMessage)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// FailureInfo
	offset = Primitives.Variable1.encode(data.failureInfo.errorMessage, buffer, offset)
	offset = Primitives.UUID.encode(data.failureInfo.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.failureInfo.transactionId, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): FeatureDisabledData {
	const state = { offset: HEADER_SIZE }

	return {
		failureInfo: {
			errorMessage: Primitives.Variable1.decode(buffer, state),
			agentId: Primitives.UUID.decode(buffer, state),
			transactionId: Primitives.UUID.decode(buffer, state),
		},
	}
}
