/**
 * KickUserAck Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface KickUserAckData {
	userInfo: {
		sessionId: Types.UUID
		flags: number
	}
}

export const id = 164
export const name = "KickUserAck"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "userInfo" entry (fixed-length parameters only)
const USER_INFO_BASE_SIZE =
	Primitives.UUID.size() + // sessionId
	Primitives.U32.size() // flags

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + USER_INFO_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<KickUserAckData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// UserInfo
	offset = Primitives.UUID.encode(data.userInfo.sessionId, buffer, offset)
	offset = Primitives.U32.encode(data.userInfo.flags, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): KickUserAckData {
	const state = { offset: HEADER_SIZE }

	return {
		userInfo: {
			sessionId: Primitives.UUID.decode(buffer, state),
			flags: Primitives.U32.decode(buffer, state),
		},
	}
}
