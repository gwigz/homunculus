/**
 * LandStatRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface LandStatRequestData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	requestData: {
		reportType: number
		requestFlags: number
		filter: Buffer
		parcelLocalId: number
	}
}

export const id = 421
export const name = "LandStatRequest"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "requestData" entry (fixed-length parameters only)
const REQUEST_DATA_BASE_SIZE =
	Primitives.U32.size() + // reportType
	Primitives.U32.size() + // requestFlags
	Primitives.S32.size() // parcelLocalId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + REQUEST_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<LandStatRequestData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.requestData.filter)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// RequestData
	offset = Primitives.U32.encode(data.requestData.reportType, buffer, offset)
	offset = Primitives.U32.encode(data.requestData.requestFlags, buffer, offset)
	offset = Primitives.Variable1.encode(data.requestData.filter, buffer, offset)
	offset = Primitives.S32.encode(data.requestData.parcelLocalId, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): LandStatRequestData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		requestData: {
			reportType: Primitives.U32.decode(buffer, state),
			requestFlags: Primitives.U32.decode(buffer, state),
			filter: Primitives.Variable1.decode(buffer, state),
			parcelLocalId: Primitives.S32.decode(buffer, state),
		},
	}
}
