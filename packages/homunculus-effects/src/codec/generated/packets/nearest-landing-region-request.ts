/**
 * NearestLandingRegionRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"

export interface NearestLandingRegionRequestData {
	requestingRegionData: {
		regionHandle: number | bigint
	}
}

export const id = 144
export const name = "NearestLandingRegionRequest"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "requestingRegionData" entry (fixed-length parameters only)
const REQUESTING_REGION_DATA_BASE_SIZE = Primitives.U64.size() // regionHandle

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + REQUESTING_REGION_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<NearestLandingRegionRequestData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// RequestingRegionData
	offset = Primitives.U64.encode(data.requestingRegionData.regionHandle, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): NearestLandingRegionRequestData {
	const state = { offset: HEADER_SIZE }

	return {
		requestingRegionData: {
			regionHandle: Primitives.U64.decode(buffer, state),
		},
	}
}
