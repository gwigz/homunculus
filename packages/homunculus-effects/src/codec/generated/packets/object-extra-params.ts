/**
 * ObjectExtraParams Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface ObjectExtraParamsData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	objectData: {
		objectLocalId: number
		paramType: number
		paramInUse: boolean
		paramSize: number
		paramData: Buffer
	}[]
}

export const id = 99
export const name = "ObjectExtraParams"
export const frequency = 2
export const compression = true

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "objectData" entry (fixed-length parameters only)
const OBJECT_DATA_BASE_SIZE =
	Primitives.U32.size() + // objectLocalId
	Primitives.U16.size() + // paramType
	Primitives.Boolean.size() + // paramInUse
	Primitives.U32.size() // paramSize

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<ObjectExtraParamsData>) {
	let size = BASE_SIZE

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += OBJECT_DATA_BASE_SIZE * (data.objectData?.length ?? 0)

	for (const item of data.objectData ?? []) {
		size += Primitives.Variable1.size(item.paramData)
	}

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// ObjectData
	offset = Primitives.U8.encode(data.objectData?.length ?? 0, buffer, offset)

	for (const item of data.objectData ?? []) {
		offset = Primitives.U32.encode(item.objectLocalId, buffer, offset)
		offset = Primitives.U16.encode(item.paramType, buffer, offset)
		offset = Primitives.Boolean.encode(item.paramInUse, buffer, offset)
		offset = Primitives.U32.encode(item.paramSize, buffer, offset)
		offset = Primitives.Variable1.encode(item.paramData, buffer, offset)
	}

	return buffer
}

export function decode(buffer: Buffer): ObjectExtraParamsData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		objectData: (() => {
			const items: ObjectExtraParamsData["objectData"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					objectLocalId: Primitives.U32.decode(buffer, state),
					paramType: Primitives.U16.decode(buffer, state),
					paramInUse: Primitives.Boolean.decode(buffer, state),
					paramSize: Primitives.U32.decode(buffer, state),
					paramData: Primitives.Variable1.decode(buffer, state),
				})
			}

			return items
		})(),
	}
}
