/**
 * RequestGodlikePowers Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface RequestGodlikePowersData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	requestBlock: {
		godlike: boolean
		token: Types.UUID
	}
}

export const id = 257
export const name = "RequestGodlikePowers"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "requestBlock" entry (fixed-length parameters only)
const REQUEST_BLOCK_BASE_SIZE =
	Primitives.Boolean.size() + // godlike
	Primitives.UUID.size() // token

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + REQUEST_BLOCK_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<RequestGodlikePowersData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// RequestBlock
	offset = Primitives.Boolean.encode(data.requestBlock.godlike, buffer, offset)
	offset = Primitives.UUID.encode(data.requestBlock.token, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): RequestGodlikePowersData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		requestBlock: {
			godlike: Primitives.Boolean.decode(buffer, state),
			token: Primitives.UUID.decode(buffer, state),
		},
	}
}
