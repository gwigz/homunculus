/**
 * RezScript Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface RezScriptData {
	agentData: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
		groupId: Types.UUID
	}
	updateBlock: {
		objectLocalId: number
		enabled: boolean
	}
	inventoryBlock: {
		itemId: Types.UUID
		folderId: Types.UUID
		creatorId: Types.UUID
		ownerId: Types.UUID
		groupId: Types.UUID
		baseMask: number
		ownerMask: number
		groupMask: number
		everyoneMask: number
		nextOwnerMask: number
		groupOwned: boolean
		transactionId: Types.UUID
		type: number
		invType: number
		flags: number
		saleType: number
		salePrice: number
		name: Buffer
		description: Buffer
		creationDate: number
		crc: number
	}
}

export const id = 304
export const name = "RezScript"
export const frequency = 2
export const compression = true

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() + // sessionId
	Primitives.UUID.size() // groupId

// base byte size for one "updateBlock" entry (fixed-length parameters only)
const UPDATE_BLOCK_BASE_SIZE =
	Primitives.U32.size() + // objectLocalId
	Primitives.Boolean.size() // enabled

// base byte size for one "inventoryBlock" entry (fixed-length parameters only)
const INVENTORY_BLOCK_BASE_SIZE =
	Primitives.UUID.size() + // itemId
	Primitives.UUID.size() + // folderId
	Primitives.UUID.size() + // creatorId
	Primitives.UUID.size() + // ownerId
	Primitives.UUID.size() + // groupId
	Primitives.U32.size() + // baseMask
	Primitives.U32.size() + // ownerMask
	Primitives.U32.size() + // groupMask
	Primitives.U32.size() + // everyoneMask
	Primitives.U32.size() + // nextOwnerMask
	Primitives.Boolean.size() + // groupOwned
	Primitives.UUID.size() + // transactionId
	Primitives.S8.size() + // type
	Primitives.S8.size() + // invType
	Primitives.U32.size() + // flags
	Primitives.U8.size() + // saleType
	Primitives.S32.size() + // salePrice
	Primitives.S32.size() + // creationDate
	Primitives.U32.size() // crc

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + UPDATE_BLOCK_BASE_SIZE + INVENTORY_BLOCK_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<RezScriptData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.inventoryBlock.name)
	size += Primitives.Variable1.size(data.inventoryBlock.description)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.groupId, buffer, offset)

	// UpdateBlock
	offset = Primitives.U32.encode(data.updateBlock.objectLocalId, buffer, offset)
	offset = Primitives.Boolean.encode(data.updateBlock.enabled, buffer, offset)

	// InventoryBlock
	offset = Primitives.UUID.encode(data.inventoryBlock.itemId, buffer, offset)
	offset = Primitives.UUID.encode(data.inventoryBlock.folderId, buffer, offset)
	offset = Primitives.UUID.encode(data.inventoryBlock.creatorId, buffer, offset)
	offset = Primitives.UUID.encode(data.inventoryBlock.ownerId, buffer, offset)
	offset = Primitives.UUID.encode(data.inventoryBlock.groupId, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.baseMask, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.ownerMask, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.groupMask, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.everyoneMask, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.nextOwnerMask, buffer, offset)
	offset = Primitives.Boolean.encode(data.inventoryBlock.groupOwned, buffer, offset)
	offset = Primitives.UUID.encode(data.inventoryBlock.transactionId, buffer, offset)
	offset = Primitives.S8.encode(data.inventoryBlock.type, buffer, offset)
	offset = Primitives.S8.encode(data.inventoryBlock.invType, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.flags, buffer, offset)
	offset = Primitives.U8.encode(data.inventoryBlock.saleType, buffer, offset)
	offset = Primitives.S32.encode(data.inventoryBlock.salePrice, buffer, offset)
	offset = Primitives.Variable1.encode(data.inventoryBlock.name, buffer, offset)
	offset = Primitives.Variable1.encode(data.inventoryBlock.description, buffer, offset)
	offset = Primitives.S32.encode(data.inventoryBlock.creationDate, buffer, offset)
	offset = Primitives.U32.encode(data.inventoryBlock.crc, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): RezScriptData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
			groupId: Primitives.UUID.decode(buffer, state),
		},
		updateBlock: {
			objectLocalId: Primitives.U32.decode(buffer, state),
			enabled: Primitives.Boolean.decode(buffer, state),
		},
		inventoryBlock: {
			itemId: Primitives.UUID.decode(buffer, state),
			folderId: Primitives.UUID.decode(buffer, state),
			creatorId: Primitives.UUID.decode(buffer, state),
			ownerId: Primitives.UUID.decode(buffer, state),
			groupId: Primitives.UUID.decode(buffer, state),
			baseMask: Primitives.U32.decode(buffer, state),
			ownerMask: Primitives.U32.decode(buffer, state),
			groupMask: Primitives.U32.decode(buffer, state),
			everyoneMask: Primitives.U32.decode(buffer, state),
			nextOwnerMask: Primitives.U32.decode(buffer, state),
			groupOwned: Primitives.Boolean.decode(buffer, state),
			transactionId: Primitives.UUID.decode(buffer, state),
			type: Primitives.S8.decode(buffer, state),
			invType: Primitives.S8.decode(buffer, state),
			flags: Primitives.U32.decode(buffer, state),
			saleType: Primitives.U8.decode(buffer, state),
			salePrice: Primitives.S32.decode(buffer, state),
			name: Primitives.Variable1.decode(buffer, state),
			description: Primitives.Variable1.decode(buffer, state),
			creationDate: Primitives.S32.decode(buffer, state),
			crc: Primitives.U32.decode(buffer, state),
		},
	}
}
