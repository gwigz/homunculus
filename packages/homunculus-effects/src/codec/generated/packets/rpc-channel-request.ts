/**
 * RpcChannelRequest Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface RpcChannelRequestData {
	dataBlock: {
		gridX: number
		gridY: number
		taskId: Types.UUID
		itemId: Types.UUID
	}
}

export const id = 413
export const name = "RpcChannelRequest"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "dataBlock" entry (fixed-length parameters only)
const DATA_BLOCK_BASE_SIZE =
	Primitives.U32.size() + // gridX
	Primitives.U32.size() + // gridY
	Primitives.UUID.size() + // taskId
	Primitives.UUID.size() // itemId

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + DATA_BLOCK_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<RpcChannelRequestData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// DataBlock
	offset = Primitives.U32.encode(data.dataBlock.gridX, buffer, offset)
	offset = Primitives.U32.encode(data.dataBlock.gridY, buffer, offset)
	offset = Primitives.UUID.encode(data.dataBlock.taskId, buffer, offset)
	offset = Primitives.UUID.encode(data.dataBlock.itemId, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): RpcChannelRequestData {
	const state = { offset: HEADER_SIZE }

	return {
		dataBlock: {
			gridX: Primitives.U32.decode(buffer, state),
			gridY: Primitives.U32.decode(buffer, state),
			taskId: Primitives.UUID.decode(buffer, state),
			itemId: Primitives.UUID.decode(buffer, state),
		},
	}
}
