/**
 * ScriptDataReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"

export interface ScriptDataReplyData {
	dataBlock: {
		hash: number | bigint
		reply: Buffer
	}[]
}

export const id = 338
export const name = "ScriptDataReply"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "dataBlock" entry (fixed-length parameters only)
const DATA_BLOCK_BASE_SIZE = Primitives.U64.size() // hash

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<ScriptDataReplyData>) {
	let size = BASE_SIZE

	// add 1 byte for the block count
	size += Primitives.U8.size()

	size += DATA_BLOCK_BASE_SIZE * (data.dataBlock?.length ?? 0)

	for (const item of data.dataBlock ?? []) {
		size += Primitives.Variable2.size(item.reply)
	}

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// DataBlock
	offset = Primitives.U8.encode(data.dataBlock?.length ?? 0, buffer, offset)

	for (const item of data.dataBlock ?? []) {
		offset = Primitives.U64.encode(item.hash, buffer, offset)
		offset = Primitives.Variable2.encode(item.reply, buffer, offset)
	}

	return buffer
}

export function decode(buffer: Buffer): ScriptDataReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		dataBlock: (() => {
			const items: ScriptDataReplyData["dataBlock"] = []
			const size = Primitives.U8.decode(buffer, state)

			for (let i = 0; i < size; i++) {
				items.push({
					hash: Primitives.U64.decode(buffer, state),
					reply: Primitives.Variable2.decode(buffer, state),
				})
			}

			return items
		})(),
	}
}
