/**
 * ScriptDialogReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface ScriptDialogReplyData {
	agentData?: {
		agentId?: Types.UUID
		sessionId?: Types.UUID
	}
	data: {
		objectId: Types.UUID
		chatChannel: number
		buttonIndex: number
		buttonLabel: Buffer
	}
}

export const id = 191
export const name = "ScriptDialogReply"
export const frequency = 2
export const compression = true

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "agentData" entry (fixed-length parameters only)
const AGENT_DATA_BASE_SIZE =
	Primitives.UUID.size() + // agentId
	Primitives.UUID.size() // sessionId

// base byte size for one "data" entry (fixed-length parameters only)
const DATA_BASE_SIZE =
	Primitives.UUID.size() + // objectId
	Primitives.S32.size() + // chatChannel
	Primitives.S32.size() // buttonIndex

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + AGENT_DATA_BASE_SIZE + DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<ScriptDialogReplyData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.data.buttonLabel)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// AgentData
	offset = Primitives.UUID.encode(data.agentData.agentId, buffer, offset)
	offset = Primitives.UUID.encode(data.agentData.sessionId, buffer, offset)

	// Data
	offset = Primitives.UUID.encode(data.data.objectId, buffer, offset)
	offset = Primitives.S32.encode(data.data.chatChannel, buffer, offset)
	offset = Primitives.S32.encode(data.data.buttonIndex, buffer, offset)
	offset = Primitives.Variable1.encode(data.data.buttonLabel, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): ScriptDialogReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		agentData: {
			agentId: Primitives.UUID.decode(buffer, state),
			sessionId: Primitives.UUID.decode(buffer, state),
		},
		data: {
			objectId: Primitives.UUID.decode(buffer, state),
			chatChannel: Primitives.S32.decode(buffer, state),
			buttonIndex: Primitives.S32.decode(buffer, state),
			buttonLabel: Primitives.Variable1.decode(buffer, state),
		},
	}
}
