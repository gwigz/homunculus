/**
 * ScriptRunningReply Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface ScriptRunningReplyData {
	script: {
		objectId: Types.UUID
		itemId: Types.UUID
		running: boolean
	}
}

export const id = 244
export const name = "ScriptRunningReply"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "script" entry (fixed-length parameters only)
const SCRIPT_BASE_SIZE =
	Primitives.UUID.size() + // objectId
	Primitives.UUID.size() + // itemId
	Primitives.Boolean.size() // running

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + SCRIPT_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<ScriptRunningReplyData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// Script
	offset = Primitives.UUID.encode(data.script.objectId, buffer, offset)
	offset = Primitives.UUID.encode(data.script.itemId, buffer, offset)
	offset = Primitives.Boolean.encode(data.script.running, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): ScriptRunningReplyData {
	const state = { offset: HEADER_SIZE }

	return {
		script: {
			objectId: Primitives.UUID.decode(buffer, state),
			itemId: Primitives.UUID.decode(buffer, state),
			running: Primitives.Boolean.decode(buffer, state),
		},
	}
}
