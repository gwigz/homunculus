/**
 * SetSimStatusInDatabase Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"
import type * as Types from "~/model/types"

export interface SetSimStatusInDatabaseData {
	data: {
		regionId: Types.UUID
		hostName: Buffer
		x: number
		y: number
		pid: number
		agentCount: number
		timeToLive: number
		status: Buffer
	}
}

export const id = 22
export const name = "SetSimStatusInDatabase"
export const frequency = 2

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "data" entry (fixed-length parameters only)
const DATA_BASE_SIZE =
	Primitives.UUID.size() + // regionId
	Primitives.S32.size() + // x
	Primitives.S32.size() + // y
	Primitives.S32.size() + // pid
	Primitives.S32.size() + // agentCount
	Primitives.S32.size() // timeToLive

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + DATA_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<SetSimStatusInDatabaseData>) {
	let size = BASE_SIZE

	size += Primitives.Variable1.size(data.data.hostName)
	size += Primitives.Variable1.size(data.data.status)

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// Data
	offset = Primitives.UUID.encode(data.data.regionId, buffer, offset)
	offset = Primitives.Variable1.encode(data.data.hostName, buffer, offset)
	offset = Primitives.S32.encode(data.data.x, buffer, offset)
	offset = Primitives.S32.encode(data.data.y, buffer, offset)
	offset = Primitives.S32.encode(data.data.pid, buffer, offset)
	offset = Primitives.S32.encode(data.data.agentCount, buffer, offset)
	offset = Primitives.S32.encode(data.data.timeToLive, buffer, offset)
	offset = Primitives.Variable1.encode(data.data.status, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): SetSimStatusInDatabaseData {
	const state = { offset: HEADER_SIZE }

	return {
		data: {
			regionId: Primitives.UUID.decode(buffer, state),
			hostName: Primitives.Variable1.decode(buffer, state),
			x: Primitives.S32.decode(buffer, state),
			y: Primitives.S32.decode(buffer, state),
			pid: Primitives.S32.decode(buffer, state),
			agentCount: Primitives.S32.decode(buffer, state),
			timeToLive: Primitives.S32.decode(buffer, state),
			status: Primitives.Variable1.decode(buffer, state),
		},
	}
}
