/**
 * StartPingCheck Packet
 *
 * This file is used to help our packet serialization and deserialization
 * process, and to create new packets on the fly.
 *
 * ⚠️ Do not edit this file manually, it is generated by the `codegen` script!
 *
 * @see {@link http://wiki.secondlife.com/wiki/Message_Layout}
 */

import type { DeepRequired } from "ts-essentials"
import * as PacketEncoder from "~/codec/lludp/packet-encoder"
import * as Primitives from "~/codec/primitives"

export interface StartPingCheckData {
	pingId: {
		pingId: number
		oldestUnacked: number
	}
}

export const id = 1
export const name = "StartPingCheck"
export const frequency = 0

const HEADER_SIZE = PacketEncoder.FREQUENCY_OFFSETS[frequency]!

// base byte size for one "pingId" entry (fixed-length parameters only)
const PING_ID_BASE_SIZE =
	Primitives.U8.size() + // pingId
	Primitives.U32.size() // oldestUnacked

// size contributed by the packet header and all FIXED-LENGTH fields
const BASE_SIZE =
	// Header
	HEADER_SIZE + PING_ID_BASE_SIZE

export function encode(sequence: number, reliable: boolean, data: DeepRequired<StartPingCheckData>) {
	const size = BASE_SIZE

	const buffer = Buffer.allocUnsafe(size)

	PacketEncoder.encodeHeader(buffer, id, frequency, sequence, reliable)

	let offset = HEADER_SIZE

	// PingId
	offset = Primitives.U8.encode(data.pingId.pingId, buffer, offset)
	offset = Primitives.U32.encode(data.pingId.oldestUnacked, buffer, offset)

	return buffer
}

export function decode(buffer: Buffer): StartPingCheckData {
	const state = { offset: HEADER_SIZE }

	return {
		pingId: {
			pingId: Primitives.U8.decode(buffer, state),
			oldestUnacked: Primitives.U32.decode(buffer, state),
		},
	}
}
